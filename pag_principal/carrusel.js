const form = document.querySelectorAll('.formulario');

const prevBtn = document.getElementById('anterior');
const nextBtn = document.getElementById('posterior');
let currentIndex = 0;

// function updateCarrusel(){
//     form.forEach(formulario, index) => {

//     }
// }